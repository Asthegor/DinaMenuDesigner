<UserControl x:Class="DinaMenuDesigner.Views.ColorPickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DinaMenuDesigner.Views"
             xmlns:common="clr-namespace:DinaMenuDesigner.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <common:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Foreground="#AAAAAA"
                   Margin="0,0,0,6"/>
        <Rectangle x:Name="ColorViewer"
                   Height="30"
                   Fill="{Binding SelectedColor, Converter={StaticResource ColorToBrushConverter}, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"/>
        <TextBlock Text="R" Foreground="#AAAAAA" Margin="0,0,0,2"/>
        <Slider Minimum="0" Maximum="255" Value="{Binding R, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        <TextBlock Text="G" Foreground="#AAAAAA" Margin="0,0,0,2"/>
        <Slider Minimum="0" Maximum="255" Value="{Binding G, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        <TextBlock Text="B" Foreground="#AAAAAA" Margin="0,0,0,2"/>
        <Slider Minimum="0" Maximum="255" Value="{Binding B, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        <TextBlock Text="A" Foreground="#AAAAAA" Margin="0,0,0,2"/>
        <Slider Minimum="0" Maximum="255" Value="{Binding A, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
    </StackPanel>
</UserControl>
