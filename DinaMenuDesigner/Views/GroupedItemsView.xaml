<UserControl x:Class="DinaMenuDesigner.Views.GroupedItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DinaMenuDesigner.Views"
             xmlns:common="clr-namespace:DinaMenuDesigner.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <common:DirectionToOrientationConverter x:Key="DirectionToOrientationConverter"/>
        <common:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
        <common:SpacingToMarginConverter x:Key="SpacingToMarginConverter"/>
        <common:BoolToThicknessConverter x:Key="BoolToThicknessConverter"/>
    </UserControl.Resources>
    <Border BorderBrush="White"
            BorderThickness="{Binding IsSelected, Converter={StaticResource BoolToThicknessConverter}, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <StackPanel Orientation="{Binding Direction, Converter={StaticResource DirectionToOrientationConverter}, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <ItemsControl ItemsSource="{Binding Items, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Content}"
                           Foreground="{Binding DisplayColor, Converter={StaticResource ColorToBrushConverter}}"
                           FontSize="{Binding FontSize}">
                            <TextBlock.Margin>
                                <MultiBinding Converter="{StaticResource SpacingToMarginConverter}">
                                    <Binding Path="Direction" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                    <Binding Path="SpacingX" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                    <Binding Path="SpacingY" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                </MultiBinding>
                            </TextBlock.Margin>
                        </TextBlock>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
