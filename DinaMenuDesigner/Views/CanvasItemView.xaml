<UserControl x:Class="DinaMenuDesigner.Views.CanvasItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DinaMenuDesigner.Views"
             xmlns:common="clr-namespace:DinaMenuDesigner.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <common:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <common:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
        <common:BoolToThicknessConverter x:Key="BoolToThicknessConverter"/>
    </UserControl.Resources>
    <Border BorderBrush="White"
            BorderThickness="{Binding IsSelected, Converter={StaticResource BoolToThicknessConverter}, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <TextBlock Text="{Binding ElementContent, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Foreground="{Binding ElementColor, Converter={StaticResource ColorToBrushConverter}, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
    </Border>
</UserControl>
