namespace DinaMenuDesigner.Common
{
    public enum MenuDirection { Vertical, Horizontal }
    public enum DinaHorizontalAlignment { Left, Center, Right }
    public enum DinaVerticalAlignment { Top, Center, Bottom }
}
